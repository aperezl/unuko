{
  "name": "@unuko/adapter-open5gs-sdm",
  "version": "0.1.0",
  "description": "Open5GS SDM Adapter for Unuko RSP",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean",
    "dev": "tsup src/index.ts --format esm --watch",
    "lint": "eslint src/**/*.ts"
  },
  "dependencies": {
    "@unuko/core": "workspace:*",
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.2.2"
  }
}
